# Codex Execution Prompt — 08 Manual Timetable with Conflict Prevention Foundation

## Phase

08 - Manual Timetable with Conflict Prevention Foundation

## Mandatory seed safety note

Keep the local smoke seed post-processing file after the main seed file in Supabase seed order. Do not weaken the local seeded Auth defaults safeguard. If seed files are changed, run a full Supabase reset and verify local smoke sign-in still works.

## Scope

Build manual timetable with class, teacher, and room conflict prevention only.

Do not implement automatic timetable generation.
